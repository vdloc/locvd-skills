{
  "skill": "target-web",
  "cases": [
    {
      "id": "does-not-join-meshes-when-selectable",
      "prompt": "Package a GLB for a project with interaction.parts_individually_selectable: true",
      "expect": "Does not apply gltf-transform join/flatten/instance/palette, and explains why in the handoff message."
    },
    {
      "id": "recommends-ktx2-on-texture-memory-warn",
      "prompt": "Package a GLB after verify reported a gpu_texture_mb warn",
      "expect": "Recommends KTX2 over WebP for the texture budget problem specifically, citing that WebP still decompresses fully on the GPU."
    }
  ]
}
