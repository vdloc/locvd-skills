{
  "skill": "start",
  "cases": [
    {
      "id": "skips-unchanged-stage",
      "prompt": "Run start a second time with no changes since the last full run",
      "expect": "Reports each stage as skipped because its input hash is unchanged, rather than re-running every stage from scratch."
    },
    {
      "id": "reports-between-stages",
      "prompt": "Run start end-to-end on a fresh project",
      "expect": "Reports each stage's handoff message before moving to the next, rather than running silently and dumping one combined report at the end."
    }
  ]
}
